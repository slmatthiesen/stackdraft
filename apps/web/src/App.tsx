/**
 * One-page app shell (U1 stub). U10 replaces this with the prompt→header
 * animation, clarify form, tier tabs, cost table, security panel, and diagram.
 */
export function App(): JSX.Element {
  return (
    <main>
      <h1>Stackdraft</h1>
      <p>Describe a system, get a safe, costed AWS design.</p>
    </main>
  );
}
